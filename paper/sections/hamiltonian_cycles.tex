\section{Hamiltonian-cycle structure}
\label{sec:hamiltonian-cycles}

The general classification below agrees with the complete certified
Hamiltonian-cycle universes of every analyzed double-ladder member, including
the prediction-locked graphs beyond order 36.  Those finite universes provide
an independent check of the cycle counts and classes proved here.

\begin{theorem}[Hamiltonian cycles of the double ladder]
\label{thm:cycle-formula}
For odd \(a,b\ge3\), the Hamiltonian cycles of \(D(a,b)\) are one all-rails
cycle, the \(ab\) cycles with one turn in each ladder, and four two-connector
exceptions.  In particular,
\[
 |\Hcal(D(a,b))|=ab+5.
\]
\end{theorem}

\begin{proof}
For a ladder of length \(m\), put binary variables \(x_{t,s}\) on rail \(s\)
of cell \(t\), \(y_t\) on rung \(t\), and \(c_{0,s},c_{m,s}\) on its endpoint
connectors.  At an internal vertex the degree-two equation is
\[
 x_{t-1,s}+x_{t,s}+y_t=2,
\]
with the analogous endpoint equations including \(c_{0,s}\) or \(c_{m,s}\).
Subtracting the equations on the two rails gives
\(d_t=-d_{t-1}\), where \(d_t=x_{t,0}-x_{t,1}\).  Thus the endpoint connector
bits determine the alternating rail difference, after which every rung is
forced.

If all four connectors are selected, \(d_0=0\), so the two rail choices agree
in every cell.  A zero rail pair forces the two bounding rungs.  Two such zero
pairs enclose a separate cycle, so a connected spanning solution has either no
zero pair or one.  The two ladders must have the same type: one straight and
one turned produces two components.  Hence there is one all-rails cycle and
one cycle for each independent pair of turn positions.

If exactly two connectors are selected, applying
\(d_t=(-1)^t d_0\) at the far endpoint and using odd \(m\) leaves the four
connector words \(1100,0011,1010,0101\).  In each case the recurrence has one
completion: one ladder uses all rungs and alternating rails, while the other
uses all rails and only the appropriate endpoint rung.  The remaining words
close a component before all vertices are reached.  The connector cut must be
crossed a positive even number of times, so zero, one, and three connectors
cannot yield a Hamiltonian cycle.  These cases exhaust the recurrence.
\end{proof}
