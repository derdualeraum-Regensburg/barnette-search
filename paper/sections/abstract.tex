We study the Hamiltonian strong edge-separation number \(\hsep(G)\).
Abstractly, it is the minimum size of a completely separating subfamily of the
Hamiltonian-cycle hypergraph of \(G\): for every ordered pair of distinct edges
\((e,f)\), some selected cycle contains \(e\) and avoids \(f\).  The original
computation reported an exhaustive certificate audit of all 22,263
non-isomorphic Barnette graphs through order 36, identifying every maximizer
using exact upper and lower certificates and excluding competitors by strict
upper certificates.  Following loss of the original external data, release
v0.9.0 newly recalculates the complete censuses through order 36, checks a
separating cover for every graph, and supplies exact lower certificates for
every potential maximizer.
At orders divisible by four in this range, the unique maximizers
form a double-ladder family related by a four-vertex square insertion.  We
define this family, classify its Hamiltonian cycles, and prove for all odd
\(a,b\ge3\) that
\[
 \hsep(D(a,b))=\frac{(a+2)(b+2)-1}{2}.
\]
Matching symbolic constructions prove the two bounds: a packing of ordered
edge requirements gives the lower bound, while a boundary-plus-parity family
of Hamiltonian cycles gives the upper bound through an antichain of incidence
signatures.  New exact graph-specific certificates confirm three larger family
members at orders 40, 44, and 48.  Those checks do not establish order-level
extremality beyond the census range; whether balanced double ladders remain
globally extremal after order 36 is open.

We also prove a constructive three-edge splice bound: for factors with finite
$h_i=\hsep(G_i)$, their splice satisfies
$\max\{h_1,h_2\}\le\hsep(G)\le(h_1-4)(h_2-4)+8$.
A refinement uses edge-covering subfamilies within each boundary state;
splicing with a cube increases $\hsep$ by at most a factor of two.
