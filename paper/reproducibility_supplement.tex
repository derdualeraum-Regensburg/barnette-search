% Companion technical record. This file is intentionally not input by main.tex.
\section*{Reproducibility supplement}

This companion records detailed identifiers and verification statistics that
are omitted from the main mathematical narrative.  Release v0.9.0 is a new
calculation with its own hashes and timestamps.

\subsection*{Certificate packages and runtimes}

The release standard-library verifier checked 333 files, all 29 order packages
from 8 through 36, the 14 nonempty censuses, every ordered edge-pair cover, all
lower certificates needed for the maxima, the global summaries, and the three
larger double ladders.  Its recorded runtime was 560.388 seconds on Windows 11
with Python 3.12.10.  This is audit metadata rather than a
hardware-independent benchmark.

\begin{center}
\scriptsize
\begin{tabular}{lll}
\toprule
package & entries & SHA-256 identifier \\
\midrule
release archive & 333 &
\nolinkurl{4c8d92a63d3180448bdbe372f41a10a5aa333cc2b40002e52084eb3cd5c08dfe}\\
release manifest & 333 &
\nolinkurl{f91aec24fea9653aeefc4ae013f0d62a5e5accb69cce6905ebefa66adbd99ec1}\\
certified sequence & 235 &
\nolinkurl{ca14800cb49c9a14ba578812671274be40efc780dd2b385cbcd5d4c565d42560}\\
order-36 subpackage & 8 &
\nolinkurl{a7e893a8ed4b7efb8c415ab60fcf0cfd5ddae36635aa4b9be94d9d0ff702d8c4}\\
$D(9,9)$ certificate & 9 &
\nolinkurl{2761749b6bac422236546dedaececbb28c678bebe9cc6a271e53a5566accce74}\\
$D(11,9)$ certificate & 9 &
\nolinkurl{fc37fa381dbe441eb8b90a99b68c9cfaacf46124c25b8ff4333c13e45e782f07}\\
$D(11,11)$ certificate & 9 &
\nolinkurl{474500eef0971a70440309b4b2378b7fa514bea862d5aa120f145c00da5fa22e}\\
\bottomrule
\end{tabular}
\end{center}

Every listed digest was rechecked without modifying its package.  The release
manifest covers the nested manifests as ordinary files, while the verifier also
checks their entries and mathematical contents.  The three graph-specific
certificates cover 3,540, 4,290, and 5,112 ordered edge requirements.

\subsection*{Order 36 and prediction lock}

The unique order-36 maximizer has canonical hash
\begin{center}
\scriptsize
\nolinkurl{2f96ada16c46cd2bd038b97d5af44f46ed14522cba1edf3fa041d66e02107bcc}.
\end{center}
Its complete universe has 68 Hamiltonian cycles.  A 49-cycle primal certificate
and a 49-requirement packing certificate prove \(\hsep=49\); all 15,373
competitors have verified covers of size at most 48.

The historical prospective predictions were locked in a read-only JSON record
with recorded SHA-256
\begin{center}
\scriptsize
\nolinkurl{e8ba381dd163c59941db81f65b71b70d6d78cdb7cfd6f6a41ba4828fe25c89fb}.
\end{center}
The prediction lock and the certificate calculation have separate provenance.
For each of \(D(9,9)\), \(D(11,9)\), and \(D(11,11)\), release v0.9.0 contains a
new complete normalized Hamiltonian-cycle universe.  The verifier passes the
graph identities, cycle universes, primal covers, matching lower certificates,
and manifests.

\subsection*{Layout study}

The layout study evaluated 12,672 candidate variants and exercised all 400
cap-selecting directed edges.  The structural face \texttt{B\_rail\_0} was
selected as the exterior face for all eleven family members.  Constrained
refinement produced horizontal ladder-A rails, boundary-following ladder B,
convex outer faces, consistent insertion orientation, and zero crossings.  The
unweighted family transition cost decreased from 13.6693 for independent
minima to 1.52964.  Independent verification passed the graph, rotation,
face-boundary, edge-set, label, crossing, composite, and manifest checks.

The external C source was neither copied nor modified.  Its recorded SHA-256
remained
\begin{center}
\scriptsize
\nolinkurl{8458e4c5fbadadd109c672233b1e5426d3de4b888ea1299eab1d9860b7e01c7f}.
\end{center}

\subsection*{Regression tests}

The v0.9.0 repository run passed 597 tests; ten environment-gated integrations
or external-package tests were explicitly skipped.
These ordinary software tests are separate from the standalone mathematical
certificate verification.
